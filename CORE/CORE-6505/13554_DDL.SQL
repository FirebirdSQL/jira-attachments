SET SQL DIALECT 3;
/* CREATE DATABASE 'LOCALHOST:IRIS_CIN' PAGE_SIZE 8192 DEFAULT CHARACTER SET WIN1252; */
COMMIT WORK;
COMMIT WORK; --NO NEED
SET AUTODDL OFF;
SET TERM ^ ;
/* Stored functions headers */
CREATE OR ALTER FUNCTION M_BINTODEC (BIN_NUMBER VARCHAR(63) CHARACTER SET WIN1252)
RETURNS BIGINT
AS
BEGIN END^
SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;
COMMIT WORK; --NO NEED
SET AUTODDL OFF;
SET TERM ^ ;
/* Stored functions bodies */
ALTER FUNCTION M_BINTODEC (BIN_NUMBER VARCHAR(63) CHARACTER SET WIN1252)
RETURNS BIGINT
AS
BEGIN END^
SET TERM ; ^
COMMIT WORK;
SET AUTODDL ON;